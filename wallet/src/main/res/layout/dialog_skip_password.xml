<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/pincode_dialog_bg"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:id="@+id/skip_password_message"
        style="@style/NormalText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/password_skip_warn"
        android:textColor="@color/text_primary"
        android:textSize="20sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:orientation="horizontal">

        <Button
            android:id="@+id/skip_password_cancel"
            android:layout_width="0dp"
            android:layout_height="54dp"
            android:layout_marginRight="8dp"
            android:layout_weight="1"
            android:background="@drawable/intro_action_button_alt_bg"
            android:elevation="6dp"
            android:fontFamily="sans-serif-condensed"
            android:text="@string/button_cancel"
            android:textAllCaps="true"
            android:textColor="@color/text_primary"
            android:textSize="16sp" />

        <Button
            android:id="@+id/skip_password_confirm"
            android:layout_width="0dp"
            android:layout_height="54dp"
            android:layout_marginLeft="8dp"
            android:layout_weight="1"
            android:background="@drawable/intro_action_button_bg"
            android:elevation="6dp"
            android:fontFamily="sans-serif-condensed"
            android:text="@string/button_skip"
            android:textAllCaps="true"
            android:textColor="@color/text_primary"
            android:textSize="16sp" />
    </LinearLayout>

</LinearLayout>
